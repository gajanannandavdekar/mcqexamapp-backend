name: Keep Render Backend Warm
on:
  schedule:
    - cron: '*/14 0-18 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -f https://mcqexamapp-backend.onrender.com/api/health